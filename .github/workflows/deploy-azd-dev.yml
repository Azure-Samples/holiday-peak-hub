name: deploy-azd-dev (entrypoint)

on:
  push:
    branches:
      - main
    paths:
      - azure.yaml
      - apps/**
      - lib/**
      - .infra/**
      - .github/workflows/deploy-azd.yml
      - .github/workflows/deploy-azd-dev.yml
  workflow_dispatch:
    inputs:
      location:
        description: Azure location
        required: true
        default: centralus
      projectName:
        description: Project prefix used by naming convention
        required: true
        default: holidaypeakhub405
      imageTag:
        description: Image tag to deploy
        required: true
        default: latest
      deployStatic:
        description: Provision static web app resources
        required: true
        type: boolean
        default: true
      uiOnly:
        description: Deploy only the UI using SWA token (skips provision and Azure login jobs)
        required: true
        type: boolean
        default: true
      apiBaseUrl:
        description: Optional API base URL for UI build override
        required: false
        default: ''
      seedDemoData:
        description: Run demo faker seed job after deployment
        required: true
        type: boolean
        default: true
      forceApimSync:
        description: Force APIM sync and smoke checks even when no changed services are detected
        required: true
        type: boolean
        default: false
      autoAllowAcrRunnerIp:
        description: Temporarily allow GitHub runner egress IP in ACR firewall during deploy and remove it afterward
        required: true
        type: boolean
        default: true

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    uses: ./.github/workflows/deploy-azd.yml
    with:
      environment: dev
      location: ${{ inputs.location || 'centralus' }}
      projectName: ${{ inputs.projectName || 'holidaypeakhub405' }}
      imageTag: ${{ inputs.imageTag || github.sha }}
      deployStatic: ${{ github.event_name == 'workflow_dispatch' && inputs.deployStatic || true }}
      uiOnly: ${{ github.event_name == 'workflow_dispatch' && inputs.uiOnly || false }}
      apiBaseUrl: ${{ inputs.apiBaseUrl || '' }}
      seedDemoData: ${{ github.event_name == 'workflow_dispatch' && inputs.seedDemoData || false }}
      deployChangedOnly: true
      forceApimSync: ${{ github.event_name == 'workflow_dispatch' && inputs.forceApimSync || false }}
      autoAllowAcrRunnerIp: ${{ github.event_name == 'workflow_dispatch' && inputs.autoAllowAcrRunnerIp || true }}
    secrets:
      AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
      AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      SWA_DEPLOYMENT_TOKEN: ${{ secrets.SWA_DEPLOYMENT_TOKEN }}
