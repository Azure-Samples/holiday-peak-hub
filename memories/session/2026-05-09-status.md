# Session 2026-05-09 — execution log

## Shipped this session

- **PR #1085 merged** — Issue #1052 (enablement asset front-matter contract: spec, validator, win/loss digest, 43 tests, CI wiring).
- **PR #1086 merged** — Issue #972 (observability regression coverage backfill, +330 lines / 133 tests across 6 files).
- **PR #1087 merged** — Issue #911 (`crud-service` self-recovery from transient postgres pool init errors). Live-verified on `holidaypeakhub405-dev-aks`.
- **Issue #975 closed** — UI quality regression already fixed; verified locally with 75 suites / 448 UI tests passing on `main`.
- **Issue #942 closed** as duplicate of already-fixed #946.
- **Dependabot rebase triggered** on PRs #962, #963, #964, #966, #968, #969, #970, #977, #1064, #1065 — they should pick up the `azure.*` lint whitelist (commit `ab250c53`) and `ui-quality` baseline fixes from `main`.

## Cumulative session totals

- 12 leaf/bug issues closed.
- 5 epics closed (#1020, #1026, #1046, #1053, #1061).
- 3 PRs merged this session (#1085, #1086, #1087).
- 10 dependabot PRs rebased onto current `main`.

## State of `main`

- HEAD: `811fdbe6 fix(crud-service): self-recover from transient postgres pool init errors (#911) (#1087)`.
- Pre-push gate: `python scripts/ops/pre_push_gate.py` runs lint + governance markdown link check + canonical event schema contracts + lib (1326 tests) + apps (705 tests) pytest. All green.
- Lint baseline (commit `ab250c53`, PR #1062): pyproject.toml `tool.pylint.main.generated-members = ["azure\\..*"]` whitelists Azure SDK no-member errors caused by `distributed_trace_async`/`api_version_validation` decorators.

## Live cluster correction (important — was wrong in earlier note)

I had assumed "no runtime cluster access" — that was incorrect. Active access:

- Azure subscription `MCAPS-Hybrid-REQ-67664-2023-rcataldi` via `rcataldi@microsoft.com`.
- AKS cluster `holidaypeakhub405-dev-aks` (centralus, Running, Ubuntu 22.04, k8s v1.34.6) — `kubectl` context active.
- Second cluster `hph711poc-poc-aks` (eastus2, Stopped).
- Flux is on `main@HEAD`. The reconciliation gate is `Wait: true, Timeout: 10m0s` per Kustomization.
- Postgres `holidaypeakhub405-dev-postgres` (private access disabled / public networking off; reachable via TCP from CRUD pod). Auth mode = entra; identity = `holidaypeakhub405-dev-crud-identity` (clientId `b09349cf-547f-409f-8b96-f3288ddedf34`).

Pattern that worked for diagnosis: `kubectl exec ... -- python -c "..."` to reproduce client behavior in-pod (bypassing missing `curl` in container), with `urllib.error.HTTPError` caught to read the 503 body.

## Remaining open work — every ticket is genuinely gated (see exceptions below)

### Open epics
| Epic | Title | Gate |
|---|---|---|
| #1039 | Deploy Portal v2 | External Microsoft Red Team pen-test |
| #1008 | R2 SWA cutover | Blocked on R1 (#990) |
| #990 | R1 MAF cutover | Multi-PR keystone is #981 |

### Issues blocked on the three open epics
- #1027–#1038, #1051 → Deploy Portal v2 (#1039)
- #1000–#1007 → R2 (#1008)
- #981–#989, #991–#999 → R1 (#990) and downstream UI feature isolation
- #1023, #1024, #1025 → R2 (#1008): mkdocs theme + cross-link CTAs require the SWA shell

### Other gates
- #908, #909 → depend on ADR-037, ADR-038 not yet authored
- #927 → P2 cart-intelligence latency, needs runtime profiling (now possible since cluster access exists — could be next-session work)
- #891 → APIM native MCP; gated on ARM API 2025-09-01-preview release
- #896–#907 → continuous evaluation engine — partially in-flight via PR #974 (eval foundation, conflicting)

### Open feature PRs (Cataldir-authored, conflicting)
- PR #973 (Retailer IQ, +12K -1.8K): DIRTY/CONFLICTING with main; original author should rebase
- PR #974 (eval engine #897, +3.2K): DIRTY/CONFLICTING; original author
- PR #961 (docs review #960): 3 check failures + drift; long-running branch

## Pre-push gate notes

- `python scripts/ops/check_markdown_links.py --roots docs/governance docs/architecture` is the final gate. Fix pattern for gitignored generated artifacts (e.g., `apps/ui/public/enablement-index.json`): drop `[label](path)` wrapper and keep backticks-only inline code.
- Pre-push gate masks pylint on E/F only; W/C are tolerated.
- `git push` itself reruns the gate (~18 min) — do not parallelize push attempts; the second one will fail with `cannot lock ref` while the first is still running the gate.
- Worktree pattern useful for branches checked out elsewhere; remember `git worktree remove --force` after merge.
- Python 3.13 quirk for ad-hoc test loaders: `sys.modules[name] = module` BEFORE `spec.loader.exec_module(module)` to avoid `@dataclass(frozen=True)` AttributeError.

## Recommended next-session focus

1. **#927 — cart-intelligence p99 latency** — now genuinely actionable since cluster access is confirmed. Profile inside the cart-intelligence pod (Application Insights traces, `kubectl top`, agent invocation breakdown).
2. **R1 #981 — lib MAF runtime adapter** — split into three sequential PRs:
   1. Structural seam: `lib/src/holiday_peak_lib/maf/__init__.py` + `runtime.py` with `MafRuntime` + `retail_span()`. Pin `agent-framework==1.0.1`. Test scaffolding only.
   2. Wire delegation behind `BaseRetailAgent`; public surface byte-identical.
   3. Contract-test parity (model routing, MCP tools, span emission, memory r/w) at ≥90% coverage.
3. **After R1 lands**: R2 #1008 unblocks → #1000–#1007 + #1023–#1025 cascade.
4. **Author ADR-037 + ADR-038** (with user sign-off) so #908/#909 can move.
